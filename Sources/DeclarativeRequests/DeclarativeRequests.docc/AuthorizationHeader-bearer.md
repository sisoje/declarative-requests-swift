# ``AuthorizationHeader/bearer(_:)``

Sets `Authorization: Bearer <token>` (RFC 6750).

## Overview

```swift
AuthorizationHeader.bearer(accessToken)
// -> Authorization: Bearer eyJhbGci...
```

- Parameter token: The bearer token.
