# ``AllowAccess/ultraConstrainedNetwork(_:)``

Toggle `URLRequest.allowsUltraConstrainedNetworkAccess`. Only applied on platforms where the property exists; on older OS versions the block is a no-op.

- Parameter value: `true` to permit ultra-constrained networks, `false` to deny.
