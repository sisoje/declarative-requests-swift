# ``RefererHeader``

`Referer` header. Set-default. (Note: the HTTP header name is misspelled by historical
convention — `Referer`, not `Referrer`.)

## Overview

```swift
Headers {
    RefererHeader("https://example.com/page")
}
```
