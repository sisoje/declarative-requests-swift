# ``RequestBuildable/body``

The blocks this value is composed of.

## Overview

Return one or more ``RequestBuildable`` values inside a `@RequestBuilder`
closure. The result builder folds them into a single transform applied in
declaration order.
