# ``DeclarativeRequestsError/badUrl``

The request URL is missing or could not be constructed from the inputs declared in the builder.

## Overview

Typically thrown by ``BaseURL`` when given an invalid string, or by ``Endpoint`` when no base URL has been set.
